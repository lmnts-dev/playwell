import { CompressedGraphMap, PrefetchGraph } from './declarations';
export declare const compressGraph: (input: PrefetchGraph, precision: number) => {
    graph: number[][][];
    graphMap: CompressedGraphMap;
};
