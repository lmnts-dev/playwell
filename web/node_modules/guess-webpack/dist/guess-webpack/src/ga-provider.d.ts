import { RoutingModule, Period, Graph } from '../../common/interfaces';
export interface Config {
    jwt?: any;
    viewId: string;
    routes: RoutingModule[];
    formatter?: (path: string) => string;
    period?: Period;
}
export declare const getReport: (c: Config) => Promise<Graph>;
