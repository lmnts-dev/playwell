import { CompressedGraphMap, PrefetchConfig } from '../declarations';
export declare const initialize: (history: History, global: any, graph: number[][][], map: CompressedGraphMap, basePath: string, thresholds: PrefetchConfig) => void;
