import { Graph, Period } from '../../common/interfaces';
export interface FetchConfig {
    auth: any;
    viewId: string;
    period: Period;
    formatter?: (route: string) => string;
    routes?: string[];
    expression?: string;
}
export declare function fetch(config: FetchConfig): Promise<Graph>;
