'use strict';

exports.__esModule = true;

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _propTypes = require('prop-types');

var _propTypes2 = _interopRequireDefault(_propTypes);

var _availableProps = require('./availableProps');

var _availableProps2 = _interopRequireDefault(_availableProps);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }

var UserAgent = function UserAgent(_ref, _ref2) {
  var ua = _ref2.ua;

  var children = _ref.children,
      returnFullParser = _ref.returnFullParser,
      props = _objectWithoutProperties(_ref, ['children', 'returnFullParser']);

  var validProps = Object.keys(props).filter(function (prop) {
    return _availableProps2.default.indexOf(prop) !== -1;
  });
  var ret = validProps.some(function (prop) {
    return ua.uaResults[prop];
  });
  var funcChildren = typeof children === 'function';

  if (validProps.length !== 0) {
    if (funcChildren) {
      return children(ret);
    }
    if (ret) {
      return children;
    }
    return null;
  }
  if (funcChildren) {
    if (returnFullParser) {
      return children(ua.parser);
    }
    return children(ua.uaResults);
  }

  if (process.env.NODE_ENV !== 'production') {
    throw new Error('UserAgent should be used with a function as a child when used without any props');
  }
  return null;
};

if (process.env.NODE_ENV !== 'production') {
  UserAgent.propTypes = _extends({}, _availableProps2.default.reduce(function (acc, cur) {
    var _extends2;

    return _extends({}, acc, (_extends2 = {}, _extends2[cur] = _propTypes2.default.bool, _extends2));
  }, {}), {
    returnFullParser: _propTypes2.default.bool
  });
}

UserAgent.defaultProps = {
  returnFullParser: false
};

UserAgent.contextTypes = {
  ua: _propTypes2.default.object
};

exports.default = UserAgent;
module.exports = exports['default'];